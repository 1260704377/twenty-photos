# 验证记录 · 0.1.0

验证日期：2026-09-15。环境：macOS 15.6 / Apple Silicon，Electron 44.3.0，Node.js 24。

## 核心逻辑：15 / 15 通过

`pnpm test`

覆盖：草稿持久化、撤销与结果修改、确认前字节不变、20 张上限、四种状态、次日延期、不足 20 张与空库、恢复原位、冲突不覆盖、外部修改保护、部分移动后重启续做、部分移动后回退、符号链接隔离、跨目录日额度、连续日历天数、磁盘离线、记录损坏、过去的今天排序、路径越界，以及失联照片仍可稍后决定。

## 真实桌面流程：通过

`pnpm test:ui`

以独立目录生成 25 张 JPEG、1 张有效 HEIC、1 个故意损坏的 PNG。通过真实 Electron 窗口进行操作；文件选择对话框在测试时返回该独立目录，其余使用正式 IPC、扫描、预览、持久化及文件操作。

验证了：选择目录 → EXIF 日期与首页 → 单张预览 → 快捷键 → 撤销 → 关闭重开续接 → 20 张结束 → 回看并修改 → 确认 → 关闭重开保存状态 → 撤销确认。

确认前对全部 27 个源文件校验 SHA-256；恢复后再次对全部源文件校验 SHA-256，结果一致。损坏图显示可跳过状态，有效 HEIC 成功预览。没有使用用户真实照片。

## 打包后的程序：通过

`node scripts/test-package.cjs`

直接启动 `release/mac-arm64/20张.app`，确认应用版本 0.1.0、真实打包模式、首页正确加载；从应用包内启动预览工作线程，分别解码 JPEG 与 HEIC；检查 800px 宽窗口无横向溢出。

## 视觉核验

已查看欢迎页、整理页、完成页、结果回看以及小窗口截图。结果页的损坏图使用明确占位图；照片区尺寸固定。截图及机器可读结果位于 `.test-output/`。

## 验证边界

- Windows 源码和构建配置已有，尚无 Windows 真机验证。
- 测试模拟了事务中断和路径冲突，未做真实断电或硬件故障测试。
- HEIC 验证使用 macOS 系统生成的样本，不代表所有相机、HDR 或多帧 HEIF 变体。
- 当前没有对数万张真实照片库进行基准测试；首次 EXIF 扫描可能较慢。
